# 问答对自动生成系统使用指南

## 📋 目录

- [功能概述](#功能概述)
- [核心组件](#核心组件)
- [快速开始](#快速开始)
- [详细使用步骤](#详细使用步骤)
- [API 参考](#api-参考)
- [测试流程](#测试流程)
- [常见问题](#常见问题)

---

## 功能概述

本模块提供基于现有文档自动生成问答对（QA Pairs）的功能，用于构建 RAG 系统的评测集。通过大模型阅读理解文档内容，自动生成高质量的用户问题和对应的文档关联关系。

### 主要功能

1. **自动问答生成**：从 PDF/TXT 文档中智能提取关键信息并生成问题
2. **批量处理**：支持遍历整个知识库目录批量生成问答对
3. **数据集管理**：自动保存和加载 JSON 格式的评测数据集
4. **质量评估**：使用生成的问答对评估检索系统的精确率和召回率

---

## 核心组件

### QAGenerator 类

**文件位置**：`qa_generator.py`

**主要方法**：

| 方法名 | 功能描述 | 返回值 |
|--------|---------|--------|
| `generate_qa_from_document()` | 从单个文档生成问答对 | `List[Dict]` |
| `build_eval_dataset_from_docs()` | 从文档列表批量生成 | `List[Dict]` |
| `build_eval_dataset_from_files()` | 从文件目录批量生成 | `List[Dict]` |
| `save_dataset()` | 保存数据集到 JSON 文件 | `None` |
| `load_dataset()` | 从 JSON 文件加载数据集 | `List[Dict]` |

---

## 快速开始

### 前置条件

确保以下依赖已安装：
- Python 3.8+
- LangChain 相关库
- 项目配置文件正确设置

### 第一步：生成问答对评测集

在项目根目录运行：
python test_测试混合检索.py qa_gen

或在 Python 代码中调用：
python from qa_generator import QAGenerator
generator = QAGenerator() dataset = generator.build_eval_dataset_from_files( num_questions=3, output_path="data/eval_dataset.json" )
print(f"生成了 {len(dataset)} 个问答对")

### 第二步：查看生成的数据

生成的文件位于 `data/eval_dataset.json`，格式如下：

json [ { "query": "小户型适合哪些扫地机器人？", "relevant_ids": ["data/选购指南.txt"], "source": "data/选购指南.txt" }, { "query": "扫地机器人如何避障？", "relevant_ids": ["data/扫地机器人100问.pdf"], "source": "data/扫地机器人100问.pdf" } ]
### 第三步：评估检索质量
bash 
python test_测试混合检索.py qa_eval 10

这将使用前 10 个问题测试检索系统的性能。

---

## 详细使用步骤

### 场景一：从零开始生成评测集

#### 步骤 1：准备文档

确保你的知识库文档位于配置的数据目录中（默认：`data/`），支持格式：
- `.txt` 文本文件
- `.pdf` PDF 文档

#### 步骤 2：初始化生成器

python from qa_generator import QAGenerator
使用默认配置（使用项目配置的 LLM）
generator = QAGenerator()
或者使用自定义 LLM
from your_module import custom_llm generator = QAGenerator(llm=custom_llm)

#### 步骤 3：生成问答对

python
方式 A：从文件目录自动生成
dataset = generator.build_eval_dataset_from_files( data_dir="data/", # 可选，默认从 chroma.yml 读取 allowed_types=(".txt", ".pdf"), # 可选，允许的文件类型 num_questions=3, # 每个文档生成 3 个问题 output_path="data/eval_dataset.json" # 输出文件路径 )
方式 B：从已有的 Document 对象生成
from langchain_core.documents import Document
documents = [ Document(page_content="...", metadata={"source": "doc1.txt"}), Document(page_content="...", metadata={"source": "doc2.txt"}), ]
dataset = generator.build_eval_dataset_from_docs( documents=documents, num_questions=3, output_path="data/eval_dataset.json" )

#### 步骤 4：验证生成结果

python
查看统计信息
print(f"总问答对数量: {len(dataset)}")
查看前 5 个示例
for i, item in enumerate(dataset[:5], 1): print(f"\n{i}. 问题: {item['query']}") print(f" 来源: {item['source']}") print(f" 相关文档: {item['relevant_ids']}")


#### 步骤 5：人工质量检查（推荐）

打开 `data/eval_dataset.json`，抽样检查 5-10 条记录：
- ✅ 问题是否清晰明确
- ✅ 问题是否能从对应文档中找到答案
- ✅ 问题是否符合真实用户口吻
- ❌ 删除重复或低质量问题

---

### 场景二：使用生成的评测集进行检索评估

#### 步骤 1：加载评测集

python from qa_generator import QAGenerator
dataset = QAGenerator.load_dataset("data/eval_dataset.json") print(f"加载了 {len(dataset)} 个测试样本")

#### 步骤 2：初始化检索系统

python from rag.vector_store import VectorStoreService
vs = VectorStoreService() vs.load_documents()
获取检索器（自动选择传统或混合检索）
retriever = vs.get_retriever()

#### 步骤 3：执行评估

python
方式 A：使用内置测试函数
from test_测试混合检索 import test_qa_evaluation
test_qa_evaluation(dataset_size=10) # 评估前 10 个问题
方式 B：手动评估
total_precision = 0 total_recall = 0
for sample in dataset[:10]: query = sample['query'] relevant_ids = sample['relevant_ids']
# 执行检索
retrieved_docs = retriever.invoke(query)
retrieved_sources = [doc.metadata.get('source', '') for doc in retrieved_docs]

# 计算命中数
import os
retrieved_basenames = [os.path.basename(doc.metadata.get('source', '')) for doc in retrieved_docs]
hits = sum(1 for rid in relevant_ids if rid in retrieved_basenames)

# 计算指标
precision = hits / len(retrieved_docs) if retrieved_docs else 0
recall = hits / len(relevant_ids) if relevant_ids else 0

total_precision += precision
total_recall += recall

print(f"问题: {query}")
print(f"  精确率: {precision:.2%}, 召回率: {recall:.2%}")
计算平均值
avg_precision = total_precision / 10 avg_recall = total_recall / 10
print(f"\n平均精确率: {avg_precision:.2%}") print(f"平均召回率: {avg_recall:.2%}")

#### 步骤 4：对比不同检索策略

python
测试传统向量检索
vs.hybrid_enabled = False traditional_retriever = vs.get_retriever()
测试混合检索
vs.hybrid_enabled = True hybrid_retriever = vs.get_retriever()
分别评估两种策略的性能

---

### 场景三：定制化生成参数

#### 调整每个文档的问题数量


python
生成更多问题（覆盖更全面，但成本更高）
dataset = generator.build_eval_dataset_from_files( num_questions=5, # 每个文档生成 5 个问题 output_path="data/eval_dataset_detailed.json" )
生成较少问题（快速测试）
dataset = generator.build_eval_dataset_from_files( num_questions=1, # 每个文档生成 1 个问题 output_path="data/eval_dataset_quick.json" )

#### 修改文档内容长度限制

编辑 `qa_generator.py`：

python class QAGenerator: def init(self, llm=None): self.llm = llm or chat_model self.num_questions_per_doc = 3 self.max_content_length = 3000 # 增加此值以包含更多上下文

#### 自定义 Prompt 模板

编辑 `qa_generator.py` 中的 `generate_qa_from_document` 方法：

python prompt = f"""请阅读以下文档片段，生成 {num_questions} 个用户可能会问的问题。
【要求】
问题必须能基于该片段内容回答
问题应模拟真实用户口吻，简短明确（不超过30个字）
问题类型多样化：
事实性问题（是什么、有多少）
方法性问题（怎么做、如何使用）
比较性问题（哪个更好、有什么区别）
场景性问题（适合什么情况、注意事项）
【文档内容】 {content}
【输出格式】 必须是严格的 JSON 数组，示例： [ {{"question": "小户型适合哪些扫地机器人？"}}, {{"question": "这款机器人的续航时间是多少？"}} ]
请只输出 JSON 数组，不要有其他文字："""

---

## API 参考

### QAGenerator 类

#### 构造函数


python QAGenerator(llm=None)

**参数**：
- `llm`：LLM 实例，默认为项目配置的 `chat_model`

**示例**：

python from qa_generator import QAGenerator from model.factory import chat_model
使用默认 LLM
gen1 = QAGenerator()
使用自定义 LLM
gen2 = QAGenerator(llm=chat_model)

---

#### generate_qa_from_document()

从单个文档生成问答对。

**签名**：


python def generate_qa_from_document( self, doc: Document, num_questions: Optional[int] = None ) -> List[Dict]

**参数**：
- `doc`：LangChain Document 对象
- `num_questions`：生成的问题数量（可选，默认使用类配置）

**返回值**：

python [ { "query": "问题文本", "relevant_ids": ["文档ID或路径"], "source": "文档来源" } ]


**示例**：

python from langchain_core.documents import Document
doc = Document( page_content="扫地机器人适合小户型使用，建议选择体积小巧、导航精准的型号。", metadata={"source": "选购指南.txt"} )
qa_pairs = generator.generate_qa_from_document(doc, num_questions=2)

---

#### build_eval_dataset_from_files()

从文件目录批量生成评测集。

**签名**：


python def build_eval_dataset_from_files( self, data_dir: Optional[str] = None, allowed_types: Optional[tuple] = None, num_questions: Optional[int] = None, output_path: Optional[str] = None ) -> List[Dict]


**参数**：
- `data_dir`：数据目录路径（默认从配置读取）
- `allowed_types`：允许的文件类型元组（默认：`.txt`, `.pdf`）
- `num_questions`：每个文档生成的问题数（默认：3）
- `output_path`：输出文件路径（默认：`data/eval_dataset.json`）

**返回值**：完整的评测数据集列表

**示例**：


python dataset = generator.build_eval_dataset_from_files( data_dir="data/", allowed_types=(".txt", ".pdf"), num_questions=3, output_path="data/eval_dataset.json" )

---

#### save_dataset() / load_dataset()

静态方法，用于保存和加载数据集。

**示例**：


python
保存
QAGenerator.save_dataset(dataset, "data/my_dataset.json")
加载
dataset = QAGenerator.load_dataset("data/my_dataset.json")


---

## 测试流程

### 完整测试步骤

#### 测试 1：基本功能测试

bash
运行所有测试
python test_测试混合检索.py

这会依次执行：
1. 混合检索 vs 传统检索对比
2. RAG 服务测试
3. 问答对生成测试
4. 检索质量评估

---

#### 测试 2：仅测试问答对生成


bash python test_测试混合检索.py qa_gen

**预期输出**：


================================================================================ 测试问答对自动生成
开始从文档生成问答对... [INFO] [QA生成] 找到 6 个文件 [INFO] [QA生成] 已加载: D:\desktop\langgragh项目\data\选购指南.txt [INFO] [QA生成] 处理进度: 1/15 [INFO] [QA生成] 从文档生成 3 个问题: 选购指南.txt ... [INFO] [QA生成] 完成！共生成 45 个问答对 [INFO] [QA生成] 数据集已保存: data/eval_dataset.json (共45条)
✓ 成功生成 45 个问答对
 
前10个问答对示例:
问题: 小户型适合哪些扫地机器人？ 来源: data/选购指南.txt 相关文档ID: ['data/选购指南.txt']
问题: 扫地机器人如何避障？ 来源: data/扫地机器人100问.pdf 相关文档ID: ['data/扫地机器人100问.pdf']
...
================================================================================ 问答对生成测试完成！

---

#### 测试 3：检索质量评估


bash
评估前 10 个问题
python test_测试混合检索.py qa_eval 10
评估前 20 个问题
python test_测试混合检索.py qa_eval 20

**预期输出**：


================================================================================ 使用生成的问答对进行检索质量评估
使用 10 个问题进行检索测试...
[1/10] 问题: 小户型适合哪些扫地机器人？ 命中: 1/1, 精确率: 0.33, 召回率: 1.00 [2/10] 问题: 扫地机器人如何避障？ 命中: 1/1, 精确率: 0.67, 召回率: 1.00 ...
================================================================================ 评估结果 (基于10个样本): 平均精确率: 45.50% 平均召回率: 82.30%


---

#### 测试 4：对比不同检索策略

修改 `test_测试混合检索.py` 中的 `test_qa_evaluation` 函数：


python def test_comparison(): """对比传统检索和混合检索的性能""" from qa_generator import QAGenerator from rag.vector_store import VectorStoreService
dataset = QAGenerator.load_dataset("data/eval_dataset.json")
test_samples = dataset[:10]

vs = VectorStoreService()
vs.load_documents()

# 测试传统检索
print("=" * 80)
print("测试传统向量检索")
print("=" * 80)
vs.hybrid_enabled = False
evaluate_retrieval(vs, test_samples)

# 测试混合检索
print("\n" + "=" * 80)
print("测试混合检索")
print("=" * 80)
vs.hybrid_enabled = True
evaluate_retrieval(vs, test_samples)
def evaluate_retrieval(vs, test_samples): 
    """评估检索性能""" 
    import os
    retriever = vs.get_retriever()
    
    total_precision = 0
    total_recall = 0
    
    for sample in test_samples:
        query = sample['query']
        relevant_ids = sample['relevant_ids']
        
        retrieved_docs = retriever.invoke(query)
        # 优化：提取检索结果的文件名进行对比
        retrieved_basenames = [os.path.basename(doc.metadata.get('source', '')) for doc in retrieved_docs]
        
        hits = sum(1 for rid in relevant_ids if rid in retrieved_basenames)
        
        precision = hits / len(retrieved_docs) if retrieved_docs else 0
        recall = hits / len(relevant_ids) if relevant_ids else 0
        
        total_precision += precision
        total_recall += recall

    avg_precision = total_precision / len(test_samples)
    avg_recall = total_recall / len(test_samples)
    
    print(f"平均精确率: {avg_precision:.2%}")
    print(f"平均召回率: {avg_recall:.2%}")


---

### 测试检查清单

在正式使用前，请确认以下事项：

- [ ] **环境检查**
  - [ ] Python 版本 >= 3.8
  - [ ] 所有依赖库已安装
  - [ ] LLM API 密钥配置正确

- [ ] **数据准备**
  - [ ] `data/` 目录下有至少 3 个文档
  - [ ] 文档格式为 `.txt` 或 `.pdf`
  - [ ] 文档内容非空且可读

- [ ] **配置检查**
  - [ ] `config/rag.yml` 中 `chat_model_name` 配置正确
  - [ ] `config/chroma.yml` 中数据路径配置正确

- [ ] **首次运行**
  - [ ] 运行 `python test_测试混合检索.py qa_gen` 无报错
  - [ ] `data/eval_dataset.json` 文件已生成
  - [ ] 文件中包含至少 10 个问答对

- [ ] **质量验证**
  - [ ] 打开 `data/eval_dataset.json` 抽样检查
  - [ ] 问题表述清晰、无重复
  - [ ] 问题与文档内容相关

- [ ] **性能评估**
  - [ ] 运行 `python test_测试混合检索.py qa_eval 10` 无报错
  - [ ] 精确率和召回率在合理范围（>30%）

---

## 常见问题

### Q1: 生成的问题质量不高怎么办？

**解决方案**：

1. **调整 Prompt**：在 `qa_generator.py` 中优化提示词，增加更明确的约束
2. **增加上下文**：提高 `max_content_length` 参数（如改为 3000）
3. **减少问题数量**：将 `num_questions` 从 3 降为 1-2，提高单个问题质量
4. **人工筛选**：生成后手动删除低质量问题

---

### Q2: JSON 解析失败怎么办？

**解决方案**：

代码已内置备用解析方案，会自动尝试：
1. 直接解析完整响应
2. 提取 `[...]` 之间的 JSON 部分
3. 按行提取包含问号的内容

如果仍然失败，检查日志中的错误信息，可能需要调整 LLM 的 temperature 参数使其输出更稳定。

---

### Q3: 生成速度太慢怎么办？

**解决方案**：

1. **减少问题数量**：

python dataset = generator.build_eval_dataset_from_files(num_questions=1)

2. **并行处理**（需要自行实现）：


python from concurrent.futures import ThreadPoolExecutor
with ThreadPoolExecutor(max_workers=3) as executor: results = executor.map(process_doc, documents)


3. **使用更快的 LLM**：切换到本地部署的轻量级模型

---

### Q4: 如何评估检索系统的好坏？

**评估指标**：

- **精确率（Precision）**：检索结果中相关文档的比例
  - > 60%：优秀
  - 40-60%：良好
  - < 40%：需要优化

- **召回率（Recall）**：找到的相关文档占所有相关文档的比例
  - > 80%：优秀
  - 60-80%：良好
  - < 60%：需要优化

**优化建议**：
- 精确率低 → 减少 `k` 值或使用重排序
- 召回率低 → 增加 `k` 值或改进嵌入模型

---

### Q5: 可以生成特定类型的问题吗？

**解决方案**：

修改 `qa_generator.py` 中的 Prompt，指定问题类型：

python prompt = f"""请生成以下类型的问题：
2 个事实性问题（是什么、有多少）
1 个方法性问题（怎么做）
1 个比较性问题（哪个更好）
文档内容： {content} ..."""

---

### Q6: 如何保存多个版本的评测集？

**解决方案**：


python
版本 1：基础版（每文档 1 个问题）
dataset_v1 = generator.build_eval_dataset_from_files( num_questions=1, output_path="data/eval_dataset_v1.json" )
版本 2：详细版（每文档 5 个问题）
dataset_v2 = generator.build_eval_dataset_from_files( num_questions=5, output_path="data/eval_dataset_v2.json" )
分别评估
test_qa_evaluation_with_file("data/eval_dataset_v1.json") test_qa_evaluation_with_file("data/eval_dataset_v2.json")

---

### Q7: 为什么评估时召回率是 0%？

**原因与解决**：
这通常是因为 QA 生成时记录的路径（如绝对路径）与检索返回的路径格式不一致导致的。
目前的代码已优化为**基于文件名（basename）进行匹配**，消除了路径前缀差异的影响。如果你发现召回率依然为 0，请检查：
1. 向量库中是否确实包含相关文档。
2. 嵌入模型是否能准确理解该问题的语义。
3. 尝试增加检索数量 `k`（在 `config/chroma.yml` 中调整）。

---



## 最佳实践

### 1. 渐进式生成

python
第 1 步：快速测试（少量文档）
dataset = generator.build_eval_dataset_from_files( data_dir="data/external/", # 只用外部测试数据 num_questions=1 )
第 2 步：确认质量后全量生成
dataset = generator.build_eval_dataset_from_files( num_questions=3 )


### 2. 定期更新评测集

当知识库文档更新时：

python
重新生成评测集
new_dataset = generator.build_eval_dataset_from_files( output_path="data/eval_dataset_20260414.json" )
对比新旧评测集的性能变化

### 3. 结合人工校验

python
自动生成
dataset = generator.build_eval_dataset_from_files()
人工标记低质量问题
filtered_dataset = [ item for item in dataset if not is_low_quality(item['query']) # 自定义过滤函数 ]
保存过滤后的数据集
QAGenerator.save_dataset(filtered_dataset, "data/eval_dataset_filtered.json")


---

## 技术支持

如遇问题，请检查：

1. **日志文件**：`logs/agent_*.log` 中的详细错误信息
2. **配置文件**：确认 `config/rag.yml` 和 `config/chroma.yml` 配置正确
3. **依赖版本**：确保 LangChain 及相关库版本兼容

---

## 更新日志

### v1.0.0 (2026-04-14)
- ✨ 初始版本发布
- ✨ 支持从 TXT/PDF 文档自动生成问答对
- ✨ 支持批量处理和自定义参数
- ✨ 内置检索质量评估功能
- ✨ 完整的测试用例和文档



